# This app is for common utilities, helpers, and abstract models
# that are shared across the entire project. It does not contain
# any concrete models itself.
# For example, cryptography functions would live in a utility
# module inside this app.
from django.db import models
